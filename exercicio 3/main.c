#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;

    printf("digite um numero: ");
    scanf("%i", &num);
    
    for(int linha = num - 1, linha >= 0, linha--)
    {
        for(int trasso = 1, trasso <= linha; trasso++)
        {
            print("_");
        }

        printf("*");

        for(int tra_ast = 1, )
    }
}
